{
  "components": {},
  "info": {
    "description": "Crypto market, liquidations, and Solana on-chain data for AI agents. Pay per call in USDC via x402 on Solana or Base. /api/sol-price and /api/btc-price also carry an MPP solana/charge challenge on the same 402. No API keys.",
    "title": "AgentFeed",
    "version": "1.0.0",
    "x-generated-from": [
      "https://x402.ochinimus.app/.well-known/x402.json",
      "https://x402.ochinimus.app/mcp (tools/list)"
    ]
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/sol-price": {
      "get": {
        "description": "Live SOL/USD spot price (multi-source: Coinbase, Kraken, Pyth Hermes fallback). The confidence and publish_time fields are null unless Pyth Hermes served the request; Coinbase and Kraken publish neither. Costs $0.001 USDC per call (x402, Solana mainnet).",
        "operationId": "get_sol_price",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch SOL price: live sol/usd spot price",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.001
      }
    },
    "/api/btc-price": {
      "get": {
        "description": "Live BTC/USD spot price (multi-source: Coinbase, Kraken, Pyth Hermes fallback). The confidence and publish_time fields are null unless Pyth Hermes served the request; Coinbase and Kraken publish neither. Costs $0.001 USDC per call (x402, Solana mainnet).",
        "operationId": "get_btc_price",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch BTC price: live btc/usd spot price",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.001
      }
    },
    "/api/funding-rate": {
      "get": {
        "description": "Current SOL and BTC perp funding rates, mark prices, open interest (Hyperliquid). Costs $0.002 USDC per call (x402, Solana mainnet).",
        "operationId": "get_funding_rate",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch funding rate: current sol and btc perp funding rates",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.002
      }
    },
    "/api/market-snapshot": {
      "get": {
        "description": "SOL+BTC prices, funding rates, and Fear & Greed in one call. Costs $0.003 USDC per call (x402, Solana mainnet).",
        "operationId": "get_market_snapshot",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch market snapshot: sol+btc prices, funding rates, and fear",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.003
      }
    },
    "/api/wallet-holdings/{wallet}": {
      "get": {
        "description": "Solana wallet holdings: native SOL, SPL tokens with USD values, NFT count (Helius DAS). Costs $0.008 USDC per call (x402, Solana mainnet).",
        "operationId": "get_wallet_holdings",
        "parameters": [
          {
            "description": "Solana wallet address (base58)",
            "in": "path",
            "name": "wallet",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Solana wallet address (base58)"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch wallet holdings: solana wallet holdings",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.008
      }
    },
    "/api/token-metadata/{mint}": {
      "get": {
        "description": "SPL token metadata: name, symbol, decimals, supply, price (Helius DAS). Costs $0.005 USDC per call (x402, Solana mainnet).",
        "operationId": "get_token_metadata",
        "parameters": [
          {
            "description": "SPL token mint address (base58)",
            "in": "path",
            "name": "mint",
            "required": true,
            "schema": {
              "type": "string",
              "description": "SPL token mint address (base58)"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch token metadata: spl token metadata",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.005
      }
    },
    "/api/liquidations": {
      "get": {
        "description": "Recent perp liquidations across Bybit (complete unthrottled tape), OKX and Binance: timestamp, long/short, size, price, USD value. Any USDT perp we record, not just majors. Costs $0.003 USDC per call (x402, Solana mainnet).",
        "operationId": "get_recent_liquidations",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": false,
            "schema": {
              "type": "string",
              "description": "SOL, BTC, ETH, XRP, DOGE, or any USDT perp e.g. SXTUSDT (omit for majors)"
            }
          },
          {
            "in": "query",
            "name": "scope",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "core",
                "all"
              ],
              "description": "core = the 5 majors (default), all = every recorded USDT perp"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "max rows, 1-100, default 25"
            }
          },
          {
            "in": "query",
            "name": "min_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "only prints >= this USD size"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch recent liquidations: recent perp liquidations across",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.003
      }
    },
    "/api/cascade": {
      "get": {
        "description": "Liquidation cascade detector for the 5 majors (SOL, BTC, ETH, XRP, DOGE): returns cascades active NOW - clustered same-side liquidations with symbol, side, USD total, prints, duration, severity (minor/major/extreme). Empty cascades array = no cascade in window. For every USDT perp we record across 3 exchanges, use get_cascade_scan. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_cascade_alert",
        "parameters": [
          {
            "in": "query",
            "name": "window",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback window seconds, 30-300, default 90"
            }
          },
          {
            "in": "query",
            "name": "min_events",
            "required": false,
            "schema": {
              "type": "number",
              "description": "min prints to qualify, default 4"
            }
          },
          {
            "in": "query",
            "name": "min_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "min summed USD, default 50000"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch cascade alert: liquidation cascade detector for the 5",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/cascade-scan": {
      "get": {
        "description": "FULL-UNIVERSE cascade scan: detects liquidation cascades across every USDT perp we record on Bybit, OKX and Binance simultaneously - not just majors. Bybit is the only complete unthrottled liquidation tape in crypto and no exchange publishes history of it, so this coverage is not available anywhere else. Returns symbol, side, USD total, prints, duration, severity. Costs $0.05 USDC per call (x402, Solana mainnet).",
        "operationId": "get_cascade_scan",
        "parameters": [
          {
            "in": "query",
            "name": "window",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback window seconds, 30-300, default 90"
            }
          },
          {
            "in": "query",
            "name": "min_events",
            "required": false,
            "schema": {
              "type": "number",
              "description": "min prints to qualify, default 4"
            }
          },
          {
            "in": "query",
            "name": "min_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "min summed USD, default 50000"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch cascade scan: full-universe cascade scan",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.05
      }
    },
    "/api/liquidation-leaders": {
      "get": {
        "description": "What is blowing up RIGHT NOW: top symbols ranked by liquidation USD across every USDT perp we record on Bybit, OKX and Binance. Per symbol: total liquidated, long vs short split, biggest single print, venue count, dominant side. The fastest read on where leverage is being flushed. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_liquidation_leaders",
        "parameters": [
          {
            "in": "query",
            "name": "window_min",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback minutes, 5-1440, default 60"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "top N symbols, 1-50, default 10"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch liquidation leaders: what is blowing up right now",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/liquidation-stats": {
      "get": {
        "description": "Liquidation aggregates for the 5 majors (SOL, BTC, ETH, XRP, DOGE): 1h and 24h totals, longs vs shorts USD split, biggest print, broken out per exchange. Costs $0.004 USDC per call (x402, Solana mainnet).",
        "operationId": "get_liquidation_stats",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch liquidation stats: liquidation aggregates for the 5",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.004
      }
    },
    "/api/positioning": {
      "get": {
        "description": "SOL+BTC positioning: long/short account ratio (retail crowding) + open interest with 1h/24h change (Bybit). Costs $0.004 USDC per call (x402, Solana mainnet).",
        "operationId": "get_positioning",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch positioning: sol+btc positioning",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.004
      }
    },
    "/api/trade-context": {
      "get": {
        "description": "Full market state in one call: SOL+BTC prices, funding, Fear & Greed, long/short positioning, open interest, and liquidation stats. The complete pre-trade picture. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_trade_context",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch trade context: full market state in one call",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/token-risk/{mint}": {
      "get": {
        "description": "SPL token rug-risk signals: mint/freeze authority status (revoked = safer), top-1/top-10 holder concentration, and risk flags. Not a honeypot/LP-lock checker. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_token_risk",
        "parameters": [
          {
            "description": "SPL token mint address (base58)",
            "in": "path",
            "name": "mint",
            "required": true,
            "schema": {
              "type": "string",
              "description": "SPL token mint address (base58)"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch token risk: spl token rug-risk signals",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/cascade-forecast": {
      "get": {
        "description": "FORWARD-LOOKING liquidation forecast, not a description of what already happened. Returns the probability that a symbol will liquidate more in the NEXT 15 minutes than its own 90th-percentile 15-minute window. Calibrated on a 28-day tape of 1.4M Bybit liquidations across 799 symbols, which cannot be reconstructed by anyone starting today because no exchange publishes liquidation history. Every answer carries the exact question, the threshold in USD, the window it read, the number of historical occurrences behind the number, and instructions for settling it yourself from the public feed. When a state has too little history the tool DECLINES rather than guessing, and says why. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_cascade_forecast",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": false,
            "schema": {
              "type": "string",
              "description": "SOL, BTC, ETH or any USDT perp e.g. SXTUSDT (default SOL)"
            }
          },
          {
            "in": "query",
            "name": "symbols",
            "required": false,
            "schema": {
              "type": "string",
              "description": "comma separated for a batch, max 20, e.g. SOL,BTC,ETH"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch cascade forecast: forward-looking liquidation forecast",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/liq-history": {
      "get": {
        "description": "HISTORICAL liquidation tape, time-bucketed: total/long/short USD, prints, biggest print per bucket. Any USDT perp or the whole recorded universe, up to 7 days back. Bybit is the only complete liq tape in crypto and no exchange publishes history of it — this data exists nowhere else. Costs $0.05 USDC per call (x402, Solana mainnet).",
        "operationId": "get_liq_history",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": false,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT, BTCUSDT (default SOLUSDT)"
            }
          },
          {
            "in": "query",
            "name": "scope",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "symbol",
                "all"
              ],
              "description": "all = whole universe"
            }
          },
          {
            "in": "query",
            "name": "hours",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback 1-168, default 24"
            }
          },
          {
            "in": "query",
            "name": "bucket_min",
            "required": false,
            "schema": {
              "type": "number",
              "description": "bucket minutes 5-1440, default 60"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch liq history: historical liquidation tape, time-bucketed",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.05
      }
    },
    "/api/liq-heatmap": {
      "get": {
        "description": "Liquidation heatmap by PRICE LEVEL from our own tape: where leverage actually got flushed in the last N hours — USD, prints, long/short split per price zone, hottest zone flagged. Real prints, not entry-price estimates. Costs $0.05 USDC per call (x402, Solana mainnet).",
        "operationId": "get_liq_heatmap",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          },
          {
            "in": "query",
            "name": "hours",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback 1-168, default 24"
            }
          },
          {
            "in": "query",
            "name": "buckets",
            "required": false,
            "schema": {
              "type": "number",
              "description": "price buckets 5-50, default 20"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch liq heatmap: liquidation heatmap by price level from our",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.05
      }
    },
    "/api/cascade-history": {
      "get": {
        "description": "PAST liquidation cascades reconstructed from our tape: clustered same-side flush events with start/end, prints, USD total, peak print. get_cascade_alert tells you NOW; this tells you what already happened, up to 72h back. Costs $0.03 USDC per call (x402, Solana mainnet).",
        "operationId": "get_cascade_history",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": false,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT, BTCUSDT (default SOLUSDT)"
            }
          },
          {
            "in": "query",
            "name": "scope",
            "required": false,
            "schema": {
              "type": "string",
              "enum": [
                "symbol",
                "all"
              ]
            }
          },
          {
            "in": "query",
            "name": "hours",
            "required": false,
            "schema": {
              "type": "number",
              "description": "1-72, default 24"
            }
          },
          {
            "in": "query",
            "name": "min_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "min event USD, default 100k (250k for scope=all)"
            }
          },
          {
            "in": "query",
            "name": "gap_s",
            "required": false,
            "schema": {
              "type": "number",
              "description": "max gap seconds within an event, default 60"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch cascade history: past liquidation cascades reconstructed",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.03
      }
    },
    "/api/squeeze-score": {
      "get": {
        "description": "FLAGSHIP: short-squeeze / long-flush score 0-100 for any USDT perp. Composite of funding, long/short crowding, 24h OI build, and liq-skew from our exclusive tape. One number that answers \"is this trade crowded and about to hurt someone\". Costs $0.1 USDC per call (x402, Solana mainnet).",
        "operationId": "get_squeeze_score",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch squeeze score: short-squeeze / long-flush score 0-100",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.1
      }
    },
    "/api/venue-liq-share": {
      "get": {
        "description": "Which venue is flushing whom: per-exchange liquidation share (Bybit/OKX/Binance) with long/short split and biggest print, any symbol or whole universe. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_venue_liq_share",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": false,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT, BTCUSDT (default all)"
            }
          },
          {
            "in": "query",
            "name": "hours",
            "required": false,
            "schema": {
              "type": "number",
              "description": "1-168, default 24"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch venue liq share: which venue is flushing whom",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/funding-cross": {
      "get": {
        "description": "Funding for ANY USDT perp across Bybit + OKX + Hyperliquid in one call, with cross-venue spread and crowding read. (get_funding_rate covers SOL+BTC only.) Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_funding_cross",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch funding cross: funding for any usdt perp across bybit +",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/funding-extremes": {
      "get": {
        "description": "Most crowded trades across every Bybit USDT perp: top most-positive and most-negative funding with annualized %, 24h price move and OI. Crowded shorts = squeeze candidates. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_funding_extremes",
        "parameters": [
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "top N each side, 1-25, default 10"
            }
          },
          {
            "in": "query",
            "name": "min_turnover_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "liquidity floor, default 1M"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch funding extremes: most crowded trades across every bybit",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/open-interest": {
      "get": {
        "description": "Open interest for ANY USDT perp: Bybit OI in base + USD with 1h/24h change, plus OKX OI. (get_positioning covers SOL+BTC only.) Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_open_interest",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch open interest: open interest for any usdt perp",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/oi-spike-scan": {
      "get": {
        "description": "Abnormal open-interest jumps across every Bybit USDT perp vs a 30min+ baseline — where new leverage is piling in, with funding and price context. Squeeze/flush precursor screener. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_oi_spike_scan",
        "parameters": [
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "top N, 1-25, default 10"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch OI spike scan: abnormal open-interest jumps across every",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/long-short": {
      "get": {
        "description": "Long/short account ratio for ANY USDT perp with 1h and 24h trend (retail crowding gauge). Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_long_short",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch long short: long/short account ratio for any usdt perp",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/basis": {
      "get": {
        "description": "Perp-vs-spot basis for any USDT pair: premium/discount %, contango/backwardation read, funding context. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_basis",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch basis: perp-vs-spot basis for any usdt pair",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/volatility": {
      "get": {
        "description": "Realized volatility for any USDT perp: 7d and 30d annualized from daily closes, plus today's range. Position-sizing input. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_volatility",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch volatility: realized volatility for any usdt perp",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/funding-history": {
      "get": {
        "description": "Funding-rate history for any USDT perp (up to 200 intervals): average, annualized, share of positive intervals — what the carry has actually been. Costs $0.005 USDC per call (x402, Solana mainnet).",
        "operationId": "get_funding_history",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "intervals, 1-200, default 30"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch funding history: funding-rate history for any usdt perp",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.005
      }
    },
    "/api/top-movers": {
      "get": {
        "description": "24h top gainers and losers across every Bybit USDT perp with a liquidity floor, funding attached. The \"what moved\" screener. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_top_movers",
        "parameters": [
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "top N each side, 1-25, default 10"
            }
          },
          {
            "in": "query",
            "name": "min_turnover_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "liquidity floor, default 1M"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch top movers: 24h top gainers and losers across every",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/orderbook-imbalance": {
      "get": {
        "description": "Bid/ask resting-liquidity imbalance within ±N bps of mid for any USDT perp: USD each side, ratio, skew read. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_orderbook_imbalance",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          },
          {
            "in": "query",
            "name": "bps",
            "required": false,
            "schema": {
              "type": "number",
              "description": "window ±bps around mid, 5-500, default 50"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch orderbook imbalance: bid/ask resting-liquidity imbalance",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/orderbook-walls": {
      "get": {
        "description": "Largest resting orders each side of the book for any USDT perp, with USD size and distance from mid. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_orderbook_walls",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          },
          {
            "in": "query",
            "name": "top",
            "required": false,
            "schema": {
              "type": "number",
              "description": "walls per side, 1-15, default 5"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch orderbook walls: resting orders each side of the book",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/whale-trades": {
      "get": {
        "description": "Large prints from the live trade tape for any USDT perp: trades over a USD threshold, buy/sell totals, net flow, dominant side. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_whale_trades",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          },
          {
            "in": "query",
            "name": "min_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "min print USD, default 100k"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "max trades returned, 1-50, default 20"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch whale trades: large prints from the live trade tape for",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/spread-arb": {
      "get": {
        "description": "Best bid/ask for a USDT perp across Bybit, OKX and Hyperliquid, with the best cross-venue edge in bps (pre-fee). Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_spread_arb",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "USDT perp symbol e.g. SOLUSDT"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch spread arb: bid/ask for a usdt perp across bybit, okx",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/token-holders/{mint}": {
      "get": {
        "description": "Top holders of any SPL token with per-account share and top1/top5/top10 concentration. Deeper cut than get_token_risk's summary. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_token_holders",
        "parameters": [
          {
            "description": "SPL token mint address (base58)",
            "in": "path",
            "name": "mint",
            "required": true,
            "schema": {
              "type": "string",
              "description": "SPL token mint address (base58)"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch token holders: top holders of any spl token with",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/wallet-activity/{wallet}": {
      "get": {
        "description": "Recent transactions of any Solana wallet, parsed human-readable: type, protocol, description, fee, failures (Helius enhanced). Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_wallet_activity",
        "parameters": [
          {
            "description": "Solana wallet address (base58)",
            "in": "path",
            "name": "wallet",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Solana wallet address (base58)"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "number",
              "description": "tx count, 1-25, default 10"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch wallet activity: recent transactions of any solana",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/priority-fees": {
      "get": {
        "description": "Solana priority-fee estimate right now, all levels (min to unsafeMax) in micro-lamports/CU, with a recommended tip. For bots that need txs to land. Costs $0.005 USDC per call (x402, Solana mainnet).",
        "operationId": "get_priority_fees",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch priority fees: solana priority-fee estimate right now",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.005
      }
    },
    "/api/jito-tips": {
      "get": {
        "description": "Jito bundle tip floor percentiles (p25-p99, SOL) — what landed bundles are actually paying, with a landing recommendation. Costs $0.005 USDC per call (x402, Solana mainnet).",
        "operationId": "get_jito_tips",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch jito tips: jito bundle tip floor percentiles",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.005
      }
    },
    "/api/sol-network": {
      "get": {
        "description": "Solana network health: recent average TPS, current slot, epoch and epoch progress. Costs $0.005 USDC per call (x402, Solana mainnet).",
        "operationId": "get_sol_network",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch SOL network: solana network health",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.005
      }
    },
    "/api/tvl": {
      "get": {
        "description": "TVL for any DeFi protocol (with 1d/7d change) or top-15 chains ranking. DefiLlama-backed. Costs $0.005 USDC per call (x402, Solana mainnet).",
        "operationId": "get_tvl",
        "parameters": [
          {
            "in": "query",
            "name": "target",
            "required": false,
            "schema": {
              "type": "string",
              "description": "protocol slug/name e.g. jito, marinade — omit for top chains"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch TVL: tvl for any defi protocol",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.005
      }
    },
    "/api/stablecoin-flows": {
      "get": {
        "description": "Total stablecoin supply with 7d/30d deltas and top stables — the macro risk-on/risk-off dial for crypto. Costs $0.01 USDC per call (x402, Solana mainnet).",
        "operationId": "get_stablecoin_flows",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch stablecoin flows: total stablecoin supply with 7d/30d",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.01
      }
    },
    "/api/dex-quote": {
      "get": {
        "description": "Live Jupiter swap quote for any SPL pair: output amount, price impact, route. The real executable price on Solana, not an index price. Costs $0.005 USDC per call (x402, Solana mainnet).",
        "operationId": "get_dex_quote",
        "parameters": [
          {
            "in": "query",
            "name": "input_mint",
            "required": true,
            "schema": {
              "type": "string",
              "description": "input mint (base58)"
            }
          },
          {
            "in": "query",
            "name": "output_mint",
            "required": true,
            "schema": {
              "type": "string",
              "description": "output mint (base58)"
            }
          },
          {
            "in": "query",
            "name": "amount",
            "required": true,
            "schema": {
              "type": "string",
              "description": "amount in raw base units of input mint"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch DEX quote: live jupiter swap quote for any spl pair",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.005
      }
    },
    "/api/peg-deviation": {
      "get": {
        "description": "Peg deviation for a tokenized US equity on Solana: on-chain DEX price vs the underlying last real trade, in bps, with 24h stats split into market-open and off-hours. Sampled every 5 minutes by our own collector; this tape exists nowhere else. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_peg_deviation",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Tokenized equity symbol e.g. CRCLx, MSTRx, COINx"
            }
          },
          {
            "in": "query",
            "name": "hours",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback 1-168, default 24"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch peg deviation: peg deviation for a tokenized us equity",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/peg-sessions": {
      "get": {
        "description": "Peg deviation broken out by trading session (open, premarket, afterhours, overnight, weekend): mean, p95, max bps and median liquidity per session, with the worst off-hours window flagged. Market-open acts as the control. Costs $0.03 USDC per call (x402, Solana mainnet).",
        "operationId": "get_peg_sessions",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "Tokenized equity symbol e.g. CRCLx, MSTRx, COINx"
            }
          },
          {
            "in": "query",
            "name": "days",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback 1-30, default 7"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch peg sessions: peg deviation broken out by trading session",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.03
      }
    },
    "/api/peg-universe": {
      "get": {
        "description": "Every tokenized US equity we track, ranked by off-hours peg risk: p95 and max deviation bps, market-open deviation as control, median liquidity. Dead pools are excluded rather than reported as perfect pegs. Costs $0.05 USDC per call (x402, Solana mainnet).",
        "operationId": "get_peg_universe",
        "parameters": [
          {
            "in": "query",
            "name": "days",
            "required": false,
            "schema": {
              "type": "number",
              "description": "lookback 1-30, default 7"
            }
          },
          {
            "in": "query",
            "name": "min_liquidity_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "filter out thinner pools"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch peg universe: every tokenized us equity we track, ranked",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.05
      }
    },
    "/api/exit-quote": {
      "get": {
        "description": "EXIT LIQUIDITY on seized collateral: what a liquidator ACTUALLY realises selling a Kamino reserve into live routing, versus the oracle price the protocol marks it at. Returns max_exitable_usd (largest clip whose liquidator margin is still positive, found by bisection, with its resolution width), the exitable fraction, the conservative bound at the 2% penalty floor, and for the nearest clip actually probed: realised USD, haircut bps and liquidator margin bps. Distinguishes a router that REFUSES to quote a token (permissioning, not illiquidity) from a book with no route (a real liquidity finding) - they are different facts and were one status until this split. A terminal verdict requires six consecutive agreeing observations from the symbol's own tape, so a single bad quote cannot produce a finding; withheld verdicts fall back to the last corroborated measurement with its age rather than returning null. Zero bad debt today does not disprove any of this - it means nobody has been forced to test it at size. Method, corroboration rules and row counts are free via get_exit_method. Costs $0.02 USDC per call (x402, Solana mainnet).",
        "operationId": "get_exit_quote",
        "parameters": [
          {
            "in": "query",
            "name": "symbol",
            "required": true,
            "schema": {
              "type": "string",
              "description": "reserve symbol e.g. SPYx, cbBTC, FWDI, CRCLx (get_exit_method lists all covered)"
            }
          },
          {
            "in": "query",
            "name": "size_usd",
            "required": false,
            "schema": {
              "type": "number",
              "description": "clip size in USD you would need to exit; the nearest MEASURED clip is returned, never interpolated"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch exit quote: exit liquidity on seized collateral",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.02
      }
    },
    "/api/eth-price": {
      "get": {
        "description": "ETH spot price in USD, aggregated across seven independent venues (CoinGecko, Coinbase, Kraken, Binance, OKX, Gemini, DefiLlama). Returns the lead figure plus every venue quote that answered, so a caller can see the spread rather than trust one exchange. Venues are ranked in a fixed declared order, not completion order, so identical market state always returns the same lead price. Costs $0.001 USDC per call (x402, Solana mainnet).",
        "operationId": "get_eth_price",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch ETH price: eth spot price in usd, aggregated across",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.001
      }
    },
    "/api/base-gas": {
      "get": {
        "description": "Current gas price on Base (chain 8453) in BOTH gwei and wei, with base fee, priority fee and block number when the node supplies them. Both units are returned because a caller asking in wei and a caller asking in gwei are asking the same question. Served from keyless public RPC with three-node fallback, so there is no API key to rotate or expire. Costs $0.001 USDC per call (x402, Solana mainnet).",
        "operationId": "get_base_gas",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch base gas: current gas price on base",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.001
      }
    },
    "/api/base-balance": {
      "get": {
        "description": "Native ETH or any ERC20 balance for an address on Base or Ethereum mainnet. decimals() and symbol() are read from the contract at request time rather than assumed, because assuming 18 reports a USDC balance a trillion times too large. Accepts a 0x address or an ENS name; ENS is resolved through two independent resolvers and the answer is used only when they agree, so a wrong address can never produce a confident balance for the wrong wallet. An unsupported chain is refused rather than silently answered from the wrong one. Costs $0.002 USDC per call (x402, Solana mainnet).",
        "operationId": "get_base_balance",
        "parameters": [
          {
            "in": "query",
            "name": "address",
            "required": true,
            "schema": {
              "type": "string",
              "description": "0x address (40 hex) or an ENS name ending .eth"
            }
          },
          {
            "in": "query",
            "name": "token",
            "required": false,
            "schema": {
              "type": "string",
              "description": "ERC20 contract address, or a known ticker: USDC, WETH, DAI, CBBTC, USDBC, CBETH, AERO, EURC. Omit for the native ETH balance"
            }
          },
          {
            "in": "query",
            "name": "chain",
            "required": false,
            "schema": {
              "type": "string",
              "description": "base (default) or ethereum"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Paid response."
          },
          "400": {
            "description": "Malformed or missing caller parameter."
          },
          "402": {
            "description": "Payment required. The challenge is carried in the PAYMENT-REQUIRED header (x402) and, on enabled routes, WWW-Authenticate (MPP). USDC on Solana mainnet."
          }
        },
        "summary": "Fetch base balance: native eth or any erc20 balance for an",
        "tags": [
          "agentfeed"
        ],
        "x-price-usd": 0.002
      }
    },
    "/api/fear-greed": {
      "get": {
        "description": "Crypto Fear & Greed index (0-100) with classification. Free.",
        "operationId": "get_fear_greed",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Free response. This endpoint is not payment-gated."
          }
        },
        "summary": "Fetch fear greed: crypto fear & greed index",
        "tags": [
          "agentfeed"
        ]
      }
    },
    "/api/last-liquidation": {
      "get": {
        "description": "FREE taster: last liquidation for SOL, BTC, ETH, XRP and DOGE (15-min delayed). Real-time via get_recent_liquidations. Free.",
        "operationId": "get_last_liquidation",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Free response. This endpoint is not payment-gated."
          }
        },
        "summary": "Fetch last liquidation: last liquidation for sol, btc, eth",
        "tags": [
          "agentfeed"
        ]
      }
    },
    "/api/exit-method": {
      "get": {
        "description": "FREE: how overhang measures exit liquidity on lending collateral, and the counts behind every paid answer - computed from the tape at request time, nothing hardcoded. Returns what is measured (the protocol's own live-refetched mark vs realisable value from sell-direction quotes at real clip sizes), the corroboration rule in plain terms (a terminal verdict needs six consecutive agreeing floor observations from the symbol's own tape; one sample is never enough; a contradicted floor buys a fresh probe rather than writing a hole), the full status vocabulary including why a router refusal and an empty book are different facts, the size-matched control design and its results, the gated sweep and row counts, covered symbols and markets, and the measured cadence. Read this before paying for get_exit_quote, and to check the claim rather than trust it. Free.",
        "operationId": "get_exit_method",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "description": "Free response. This endpoint is not payment-gated."
          }
        },
        "summary": "Fetch exit method: how overhang measures exit liquidity on",
        "tags": [
          "agentfeed"
        ]
      }
    }
  },
  "servers": [
    {
      "url": "https://x402.ochinimus.app"
    }
  ]
}
